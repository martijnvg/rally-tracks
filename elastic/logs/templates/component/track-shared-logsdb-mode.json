{
    "template": {
      "settings": {
        "index": {
          "codec": "best_compression"
        {% if index_mode %}
            ,"mode": {{ index_mode | tojson }}
        {% endif %}
        }
      }
    }
  }