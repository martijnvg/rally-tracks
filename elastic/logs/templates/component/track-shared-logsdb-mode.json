{
    "template": {
      "settings": {
        "index": {
          "codec": "default"
        {% if index_mode %}
            ,"mode": {{ index_mode | tojson }}
        {% endif %}
        }
      }
    }
  }